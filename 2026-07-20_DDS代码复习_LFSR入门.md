# 工作日志 - 2026年7月20日

## DDS 例程复习 + 技术方案梳理 + LFSR 入门（实际时间：7月19日 02:xx~04:14，跨天至7月20日凌晨）

### 完成事项

- **确认7月19日实战对话的成果**：Tier 1 五个模块+三个testbench已全部仿真通过（双指数核脉冲500μs波形），工程位于 `D:\.newmam_project_all\`
- **澄清两个对话的分工**：7月19日对话=刷实战任务；本对话=FPGA学习，理解原理
- **DDS_Module.v 逐行拆解**：对照已学知识（Q8计数器、Q11状态机、case）逐块分析相位累加器、LUT地址取高位、模块实例化
- **新概念讲解**：输入打一拍 `Fword_r <= Fword`（防亚稳态，用<=读干净值）、模块实例化 `rom_sine rom(.端口(信号))`（类比C的调库）
- **PPT技术方案梳理**：对比PPT总路线和Tier 1已完成项，确认PL端待学模块：LFSR随机数、Box-Muller高斯噪声

### 关键技术概念澄清

#### "打一拍"的本质
- 不是求平均值、不是数学运算
- 就是一个 D 触发器：clk 沿把此刻 Fword 的值锁存到 Fword_r
- Fword_r 总比 Fword 慢一个 clk 周期（20ns 延迟换取信号稳定）
- 连接了之前学过的 `<=` 读旧值概念

#### 模块实例化 = C 语言的调库
```verilog
rom_sine rom(.clka(Clk), .addra(addr), .douta(data))
// 等价于C：int data = rom_sine(Clk, addr);
```

#### 两个对话的确认分工
- 7月19日实际任务对话（bc48e6c0...jsonl）：写代码、跑仿真、出结果
- 本对话：理解原理、搞懂语法、巩固基础

### PPT 技术路线 vs Tier进度

| PPT模块 | Tier 1 | 说明 |
|------|:--:|------|
| DDS 相位累加器 | ✅ dds_core.v | 32bit + 12bit相位偏移 |
| 指数衰减 BRAM LUT | ✅ exp_lut.v | 4096×14bit ROM |
| 双通道 DAC 驱动 | ✅ dac_driver.v | AD9767 14bit 并口 |
| 波形状态机 | ✅ waveform_fsm.v | OFF/ONCE/PERIOD 三模式 |
| **LFSR 随机数** | ❌ | 本次开始学习 |
| **Box-Muller 高斯噪声** | ❌ | 待学 |
| AXI-Lite 寄存器 | ❌ | PS端（不在本对话范围）|

### LFSR 入门（学了一半）

**已讲概念**：
- D 触发器存 1 bit，N 个串排 = N bit 移位寄存器
- XOR（异或）= 两输入不同输出 1，相同输出 0（C语言 `^`）
- 反馈 = 取几个输出位 XOR 后送回输入端
- LFSR = 移位寄存器 + XOR 反馈 → 生成伪随机数序列
- 32 位 LFSR 最多产生 2³²-1（约42亿）个不同值

**未消化部分**：LFSR 整体运转机制、为什么选特定位做 XOR

### 下一步
- LFSR 继续消化理解
- Box-Muller 高斯噪声生成
- 等板子到了 → Tier 1 综合+烧录+示波器实测
