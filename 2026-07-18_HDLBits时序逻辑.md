# 工作日志 - 2026年7月18日

## HDLBits 时序逻辑题 Q6~Q8（实际时间：7月18日 02:02~02:50）

### 完成事项

- **HDLBits Q6: Dff（D触发器）** — 理解 `always @(posedge clk)` 的本质就是 D 触发器：clk 沿时 q 拷贝 d 的值
- **HDLBits Q7: Dff8ar（8位带复位寄存器）** — 异步复位写法：`always @(posedge clk, posedge reset)`
- **HDLBits Q8: Count10（十进制计数器 0~9）** — 计数器 = DDS 相位累加器的简化版
- **Verilog 语法纠偏** — 独立写代码暴露了以下错误

### 错误记录与纠偏

| # | 错误 | 正确 |
|:--:|------|------|
| 1 | `assign q <= d;` 写在 always 里 | `assign` 只能单独写在 module 里，always 里直接用 `<=` |
| 2 | 两个 `always` 块驱动同一个 `q` | 一个 reg 只能在一个 always 里赋值（会报 Multiple Driver Nets）|
| 3 | `always @(posedge rst)` 和 `always @(posedge clk)` 分开写 | 异步复位合并写：`always @(posedge clk, posedge reset) begin if(reset)... end` |
| 4 | `always` 块写在 `endmodule` 外面 | always 必须在 module 和 endmodule 之间 |
| 5 | 端口列表用分号：`input clk ;` | 应该用逗号：`input clk,` |
| 6 | `output [3:0];` 没写端口名 | 应该是 `output [3:0] q` |
| 7 | `if(posedge clk)` 写在 if 里 | `posedge` 只能出现在 `always @()` 敏感列表中，不能放在 if 判断里 |
| 8 | always 驱动的 output 写成了 `output [7:0] q` | 应该写 `output reg [7:0] q` |

### 语法答疑

| 问题 | 结论 |
|------|------|
| `begin` 必须和 `always` 同行吗？ | 不需要，换行写 `begin` 完全等价 |
| 两份代码能出现两个 `module top_module` 吗？ | 同一个工程不行，HDLBits 每题独立所以无事 |
| `always@()` 和 `always @()` 有区别吗？ | 没区别，空格和换行不影响 Verilog |

### 关键理解

- **D 触发器**（Q6）：`q <= d;` → 每个 clk 沿 q 变成 d 此刻的值，慢一拍
- **异步复位**（Q7）：`always @(posedge clk, posedge reset)` → reset 优先级最高，if(reset) 写最前
- **计数器**（Q8）：`q <= q + 1;` → 这就是 DDS 相位累加器的核心逻辑

### 下一步
- ~~继续 HDLBits 时序逻辑：Count1to10 / Countslow（Q9~Q10）~~
- 然后进入状态机题目，为 DDS 波形引擎做准备

---

## HDLBits Q10 Countslow + Fsm1 引入（实际时间：7月18日 03:16~03:17）

### 完成事项

- **Q9 Count1to10 跳过**（与 Q8 重复，只改范围不学新东西）
- **Q10 Countslow** — 带使能信号的慢速计数器。用户两次尝试均犯同一错误：把 slowena 当成时钟边沿触发

### Q10 关键纠偏

| 错误 | 正确 |
|------|------|
| `always @(posedge clk, posedge slowena)` | `always @(posedge clk)` — slowena 不是时钟！ |
| slowena=0 时 `q <= 0` | slowena=0 时**什么都不写** = 保持原值 |

**核心概念**：clk 是唯一的触发源。slowena 是"开关"——clk 每拍来的时候检查一下开关开着没，不是"踩一下开关就触发"。

**硬件本质**：
```verilog
always @(posedge clk)        // clk = 发动机每转一圈
    if (slowena)              // slowena = 顺便看一眼油门踩着没
        q <= q + 1;
// 不写 slowena=0 = 信号不变 = 触发器输出端保持
```

### 额外答疑
- `else if(slowena == 0) q <= 0;` 语法合法但功能不对——slowena 关掉时应保持值，不应归零
- HDLBits Q9 跳过确认（仅改范围，无新知识点）

### Fsm1 引入
给出了两状态状态机的题目描述和两段式写法模板（状态寄存器 + 次态组合逻辑），待用户下次继续。

### 下一步
- HDLBits Q11~Q13 状态机题目（三段式 FSM）
- 完成后进入 DDS 波形引擎

---

## HDLBits Q11 Fsm1 状态机实战（实际时间：7月18日 17:11 ~ 7月19日 03:11）

### 完成事项

- **VSCode 自动补全排查**：逐一检查了编辑器和扩展设置，最终确认是 `editor.inlineSuggest.enabled` 导致半透明 ghost text
- **Q11 Fsm1 状态机**：用户首次尝试，使用两段式写法填框架

### Q11 新语法从零讲解

| 语法点 | 说明 |
|------|------|
| `localparam A = 0;` | 给常数取名，等价于 `#define`。状态编码可用多位 |
| `reg state;` | 不加位宽默认 1 bit（两个状态够用）|
| `case (state) ... endcase` | 等价于 C `switch(state) { }`，`endcase` 代替 `}` |
| `always @(*)` | 组合逻辑块，读到的信号变了就立刻重算 |
| `always @(posedge clk)` | 时序逻辑块，只在 clk 沿触发 |
| `assign out = (state == B);` | 等价于 `out = (state==B) ? 1 : 0;` |

### 用户代码错误

| 行 | 错误 | 正确 | 原因 |
|:--:|------|------|------|
| 次态逻辑 | `always @(posedge clk)` | `always @(*)` | 次态逻辑是组合逻辑，不能加时钟 |
| 状态寄存器 | `state <= 1;` | `state <= next_state;` | 写死 1 丢失了"根据 in 决定跳转"的逻辑 |

### always @(*) 深入答疑

**为什么 state/in 变了 next_state 就重算？** 编译器扫描 always @(*) 块内读了哪些信号，自动构建敏感列表：`always @(state, in)`。硬件本质：state 和 in 连着组合逻辑电路，左边电平变 → 右边瞬间跟着变，没有触发器挡着。

**两段式分工**：
- `always @(posedge clk)` — 管"何时更新"（clk 沿拷过去）
- `always @(*)` — 管"更新成什么"（根据 state+in 计算）
