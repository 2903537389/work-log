# 工作日志 - 2026年8月8日

## 模拟国赛答辩准备（实际时间：8月8日 01:26~02:57）

明天（今天）就要去国赛复赛了，晚上让 AI 陪我模拟答辩现场。我先完整介绍了一遍作品——基于 ESP32-S3 的多节点分布式用电监测系统，讲完演示流程后，AI 以评委视角指出了三个演示脚本里的致命雷区：

1. **卷发棒识别跳变**：演示里卷发棒会从"大功率发热电器"变"小功率发热电器"再变"小功率电子设备"——评委必然问"为什么同一个电器识别结果在变"，而且"电子设备"是错误分类，直接打脸"PF 识别发热电器"的核心卖点
2. **2A 限流演示可能不触发**：卷发棒热态才 0.27A，2A 过流保护根本不会动作，现场会翻车
3. **功率解释话术**：不能说"剔除了噪声"，正确说法是"同步采样算有功功率 P=Σ(v·i)/N = U·I·cosφ，电压×电流是视在功率"

随后 AI 扮演评委追问了 6 个技术细节（PLC 组网、NILM 稳定性、电弧检测可信度、保护动作速度、有功功率计算、电压精度），我对照标准答法核对了自己的回答，重点补强了电弧检测和 NILM 稳定性的答法。

## PF 阈值实测标定（实际时间：8月8日 02:57~03:13）

答辩准备告一段落后，决定上真机实测数据，确定 NILM 的 PF 阈值到底设多少合适。

接上主控（COM18，接手机充电器）和卧室（COM19，接卷发棒）的串口，持续观察。系统没有命令直接打印 PF，就通过 `nilm` 命令的识别结果 + 功率/电流反推 PF = P/(V×I)（这正是分类器实际用的 `feat.pf`）。

实测结果：

| 设备/状态 | 功率 | PF | 旧阈值0.75识别 |
|-----------|------|-----|--------------|
| 手机充电器 | 27W | 0.52 | 小功率电子设备 ✓ |
| 卷发棒 高档 | 97~257W | 0.76~0.78 | 发热电器 ✓ |
| 卷发棒 中档 | 47~54W | 0.74~0.77 | 发热/电子跳变 |
| 卷发棒 低档 | 38~46W | 0.71~0.74 | 电子设备 ✗ |

关键结论：**充电器 PF 0.52 与卷发棒 PF 0.71+ 之间存在约 0.2 的天然间隔**，把阈值从 0.75 降到 0.70 就能让卷发棒全档位判发热、充电器仍判电子。同时确认了 H3+H5 对可控硅调温负载无效（卷发棒 H3+H5 全程 24%+，远超 12%，PF 才是有效判据）。

## 阈值修改与三节点重烧（实际时间：8月8日 03:13~03:44）

把 `components/algo_common/nilm_classifier.c` 的阻性判据从 `pf > 0.75f` 改成 `pf > 0.70f`，辅助判据改为 `pf > 0.65f && h35 < 0.12f`，并在 `NilmClassifier_Predict` 里加了 PF 打印（LOGD→LOGI）方便现场标定。

**踩了一个大坑**：用 `flash_slave.ps1`（内部是 `idf.py flash`）烧录，显示 `[OK]` 但实际根本没写进设备——用 esptool 读回设备 flash 对比，前 16 字节都和 bin 对不上，固件还是旧的。改成手动 `esptool write_flash` 并看到 `Hash of data verified` 才算真正烧进去。

三节点全部手动烧录成功（Hash verified）：主控 CCO（COM18）、卧室 STA（COM19）、客厅 STA2（COM11）。

烧录后验证：卷发棒 46~65W 全档位 PF 0.706~0.748，全部判"小功率发热电器"，零误判；充电器 PF 0.52 稳定判"小功率电子设备"。阈值 0.70 完美解决识别跳变问题。

## 踩坑记录（答辩和开发都要注意）

1. **idf.py flash 假成功**：`flash_master.ps1`/`flash_slave.ps1` 烧录显示 [OK] 但设备 flash 实际没更新。以后烧录用手动 esptool 并确认 `Hash of data verified`。
2. **零点校准坑**：从机上电后如果继电器被自动打开（Web 残留 ON 状态下发），功率零点校准（需要空载）被跳过 → P 恒为 0 → NILM 全部失效。正确顺序：上电 → 等启动完成（空载校准）→ 再开继电器。
3. **卷发棒低档 PF 低**：可控硅调温负载功率越低 PF 越低，0.70 阈值下 46W+ 稳判发热，但更低档位（<40W）PF 可能跌破 0.70。答辩演示建议让卷发棒保持高档位。
