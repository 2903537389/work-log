# 工作日志 - 2026年7月29日

## 概览

今天完成了 Tier 3 阶段 C1: Vivado Block Design 基础任务——ZYNQ7 PS + AXI-Lite + PL 波形集成。全脚本 TCL 一条龙，综合 + P&R 全部通过，时序 WNS +1.104ns / WHS +0.025ns，资源占用 LUT 7.35% / FF 5.53%，为下一步 Qt 侧对接和 bitstream 生成打好地基。

昨天晚上 DeepSeek 代做的 BD 因错误 part (xc7k70 Kintex) + 反复补丁导致工程崩坏，今晚决定推倒重来。

## C1 推倒重建（实际时间：07-29 00:12 ~ 00:30）

**问题定位：**
- DeepSeek 之前搭的工程 `newmam_signal_gen.*` 用了错 part xc7k70tfbv676-1（Kintex-7），导致 PS7 IP 锁死，之后所有补丁都是绕坑不是解坑
- 反复的 `apply_bd_automation` + `open_bd_design` 让 BD 状态混乱
- 最后 Vivado 内部异常 crash

**决策：**
- 独立 Tier 3 工程 `vivado_proj/tier3_zynq/`，不污染 Tier 1/2 已验证工程 `newmam_signal_gen`
- 保留 `scripts/create_project.tcl`、`scripts/pre_hook_bitstream.tcl`、`reference/BX71_Linux_Base/` 三份可复用产物
- 删除 `vivado_proj/newmam_signal_gen.*` + `scripts/c1_build_bd.tcl` + `scripts/setup_tier3_bd.tcl`
- **方案 A 全脚本 TCL**：不用 GUI 手工连线，脚本失败可重跑

## C1: 全脚本 TCL 搭 BD（实际时间：07-29 00:30 ~ 00:50）

**新脚本 `c1_setup_tier3.tcl` 关键改进（vs DeepSeek 老版）：**

| 修正点 | 原因 |
|--------|------|
| 建工程也在脚本里（一条龙） | 老脚本假设工程已存在，接续复杂 |
| 独立工程 `tier3_zynq/` | 不污染 Tier 1/2 |
| **3 个 xlconstant (32/8/1 bit)** | 老脚本用一个 32b GND 接所有位宽 → 宽度不匹配崩 |
| `validate_bd_design` 显式校验 | 老脚本无校验，出问题延到综合才发现 |
| 打印 AXI 地址分配 | Qt 侧 iregister_bus 需要基址 |
| 旧 `pl_top.xdc` 显式 disable | Tier 1/2 引脚约束会污染 Tier 3 综合 |
| 端口名去 `_0` 后缀 | 引脚约束更清爽 |
| AXI GPIO 用 `C_IS_DUAL {0}` | 老脚本 `C_GPIO2_WIDTH {0}` 不是合法参数 |

**执行结果一次成功：**
- BD 名沿用 `system`（参考 BD 命名）
- 5 个 IP 全部创建：PS7 + AXI Interconnect + AXI GPIO + proc_sys_reset + 3 个 xlconstant + axi_waveform_ctrl_0（模块引用）
- 时钟树 FCLK_CLK0 → 所有 aclk
- 复位树 FCLK_RESET0_N → proc_sys_reset → 所有 aresetn
- AXI 总线：PS→IC→(GPIO + waveform_ctrl)
- 浮空输入接对应位宽 GND
- 只有 2 个无害 WARNING（GPIO override + CLK_DOMAIN 属性提示），零 ERROR

**AXI 地址分配（Qt 侧要用）：**
- `axi_gpio_ec11` → **0x4120_0000** (64KB)
- `axi_waveform_ctrl_0` → **0x43C0_0000** (64KB)

## C1.5: 生成 Wrapper（实际时间：07-29 00:50 ~ 01:00）

`c1_5_wrap.tcl`：`generate_target all` + `make_wrapper -top`，自动 add_files 到工程。

Wrapper 端口列表（47 个），关键分组：
- PS 侧 21 个 inout（DDR + FIXED_IO）
- EC11 输入 1 个（ec11_gpio_in[2:0]）
- 波形参数 output 25 个（§1 基础 14 + §2 泊松 4 + §3 能谱 8 + §4 核素 1）

**关键观察：**
- Wrapper **没有暴露** FCLK_CLK0 / FCLK_RESET0_N / M_AXI_GP0 相关信号——它们都在 BD 内部消化了
- 这意味着 `tier3_top.v` 里所有 AXI 逐个信号 wire 都是多余的

## C1.6: 重写 tier3_top.v + 新建 tier3_top.xdc（实际时间：07-29 01:00 ~ 01:12）

**tier3_top.v v2 关键差异：**
- Wrapper 名字改为 `system_wrapper`（BD 名 = system）
- 删除 M_AXI_GP0 逐个信号（BD 内消化）
- 删除 `axi_waveform_ctrl` 例化（BD 里已含 `axi_waveform_ctrl_0`）
- §2/§3/§4 参数悬空处理（`pl_top` 只用 §1，其他悬空 Verilog 允许）
- EC11 采用方案 M：`ec11_gpio_in` 接 `3'b111` 常量占位，等引脚定后再引出

**跨时钟域决策 - 方案 B（直接跨域 + set_false_path）：**
- AXI 参数寄存器时钟 = PS FCLK_CLK0 (~100MHz，实际综合是 50MHz)
- pl_top 波形引擎时钟 = clk_50m → MMCM → 125MHz
- 参数是缓变配置（Qt 拧一下才写一次），metastable 值只影响一次采样
- `tier3_top.xdc` 里 `set_false_path -from *axi_waveform_ctrl*reg_*_reg*/C -to u_pl/*`

**其他选项被否决：**
- A（加 CDC 同步器）：26 个参数各加 2FF ~100 行，太重
- C（统一时钟域）：破坏 Tier 2 已验证的 MMCM，太危险

## C1.7: 综合失败 → 修复 → 通过（实际时间：07-29 01:12 ~ 01:26）

**首次综合失败：**
```
[Synth 8-439] module 'clk_wiz_0' not found ["pl_top.v":57]
[Synth 8-6156] failed synthesizing module 'pl_top' ["pl_top.v":15]
```

**根因：** `c1_setup_tier3.tcl` 只 add_files RTL，没有 create_ip；老工程的 `create_project.tcl` 里有 `create_ip clk_wiz` 但没迁移到 Tier 3 脚本。

**修复 `c1_fix_ip.tcl`：**
- 只补 `clk_wiz_0`（Clocking Wizard 50→125MHz）
- 不补 `rom_exp_pulse`：`exp_lut.v` 是 Tier 1 死代码，top 追依赖不会触及

**重跑综合 + 隐含跑了 impl 全通过：**

**时序（Post-Route）：**
- WNS = +1.104 ns（Setup 过关，1.1ns 余量）
- WHS = +0.025 ns（Hold 过关，25ps 余量偏小但达标）
- Design State: **Routed**（距离 bitstream 只差 write_bitstream）

**资源占用（xc7z010 极其宽裕）：**
| 资源 | 使用 | 总量 | Util% |
|------|------|------|-------|
| LUT | 1293 | 17600 | **7.35%** |
| FF | 1946 | 35200 | **5.53%** |
| Slice | 644 | 4400 | 14.64% |

**时钟树识别正确：**
- `clk_fpga_0` = 50MHz（PS FCLK_CLK0）
- `clk_50m` = 50MHz（板载晶振）
- `clk_out1_clk_wiz_0` = **125MHz**（MMCM 输出）
- `clkfbout_clk_wiz_0` = 20ns（MMCM 反馈）

**发现：** AXI 时钟实际是 50MHz（参考 BD 默认值），不是 100MHz。对 CDC 更友好。

## 今日重要收获

1. **推倒重来比修补更快** — DeepSeek 那份错工程已经积累太多不可见状态污染，直接删掉全脚本重建，1 小时搞定 C1
2. **TCL 全脚本比 GUI 更适合 AI 协作** — 不需要 AI 操作鼠标，脚本失败可重跑，state 可复现
3. **BD 里 xlconstant 位宽必须匹配** — 一个 GND 接不同位宽的输入是 Vivado 硬性拒绝的
4. **wrapper 只暴露 make_bd_pins_external 的 pin** — 内部消化的 pin（比如 M_AXI_GP0）不会出现在 wrapper 端口里
5. **从零建 Vivado 工程时要检查 create_ip 迁移** — 新工程别忘了 pl_top 依赖的 IP 也要建

## 下一步

- 归档 & git commit（work-log 仓库 + 主项目待建独立仓库 xinman-2026-nuclear-pulse-gen）
- 可选：`launch_runs impl_1 -to_step write_bitstream` 生成 .bit 文件
- 板子已连局域网，等 IP + 密码后可以远程操作调试

---

## 追加：W2 → P2 → P3 → P4 全流程上板（实际时间：07-29 02:00 ~ 03:50）

第一次归档之后没歇，紧接着做完了从 bitstream 上板到 Qt 面板在 HDMI 屏跑起来的完整闭环。今天从 C1 综合到 Qt 面板显示，共 3.5 小时打通全链路。

### W2: PL bitstream 上板（02:00 ~ 02:30）

- Vivado `launch_runs impl_1 -to_step write_bitstream`，通过 `set_property STEPS.WRITE_BITSTREAM.ARGS.BIN_FILE true` 同时产出 `.bit` 和 `.bin`（各 2MB）
- **踩坑 A**：改 run property 后 write_bitstream 立刻 out-of-date，必须 `reset_run impl_1 -from_step write_bitstream` 才能 launch
- **踩坑 B**：Vivado 默认 `.bin` 是 non-swapped（给 JTAG/QSPI 用），但 Xilinx Zynq `fpga_manager` 通过 PCAP 硬件加载要求 **byte-swapped .bin**
  - 报错：`Invalid bitstream, could not find a sync word. Bitstream must be a byte swapped .bin file`
  - 修复：Python 一行 `b''.join(data[i:i+4][::-1] for i in range(0, len(data), 4))` 反转每 4 字节
- **踩坑 C**：板子 `/lib/firmware/` 目录不存在，`mkdir -p` 手工建；`/tmp` 是 tmpfs 重启丢
- scp byte-swapped .bin → `/lib/firmware/tier3_top.bin` → `echo tier3_top.bin > /sys/class/fpga_manager/fpga0/firmware`
- 验证 state = **operating** ✅，PS↔PL 通道打通

### P2: 师兄 WSL SSH 打通（02:30 ~ 02:45）

师兄发来 `qt-dev-ssh-handover.md` + `colleague_key`，SSH `shay@192.168.6.154:2222`。

**探明师兄 WSL 环境**：
- Ubuntu 26.04 + WSL2 kernel 6.18.33.2
- 交叉编译器 Linaro GCC 7.3.1 for `arm-linux-gnueabihf`
- qmake for **Qt 5.9.6**，sysroot 660MB 完整（13 个 libQt5*.so + 全部 headers）
- 部署脚本 `~/zynq-deploy.sh`，用 sshpass 走 relay 双跳到板子

**意外发现**：板子 `/lib/` 里其实**已经预装了 Qt5.9.6 库全套**（13 个 .so），P3 编好的 exe 直接 scp 到板子就能跑，不需要打包 Qt 库。之前探测板子环境时用 `ldconfig -p | grep -ci qt5 = 0` 是索引问题，库实际存在。

### P3: 交叉编译 + 部署到板子（02:45 ~ 03:15）

- 修 `mainwindow.cpp:373` 的 `QRandomGenerator`（Qt 5.10+ 才有）→ 加 `#if QT_VERSION >= QT_VERSION_CHECK(5,10,0)` 宏保护，5.9 fallback 到 `qrand()`
- 源码 tar+scp 到师兄 WSL `~/simmax_qt/`（21 个文件）
- **踩坑 D**：`qmake` 生成的 Makefile 里 `UIC = /home/shay/zynq-qt-tools/bin/uic`，但师兄工具是**扁平布局**（uic 直接在根目录，无 bin/ 子目录），make 报 `No rule to make target '../zynq-qt-tools/bin/uic'`。写了情况说明放桌面（`ZYNQ_Qt5交叉编译_uic路径问题_情况说明_2026-07-29.md`）给师兄，师兄选**方案 B**：`mkdir bin && ln -s ../{uic,moc,rcc} bin/` 补软链接。
- `make -j16` 一次通过，产出 `simmax_panel` = 143 KB ARM 32-bit LSB executable
- **踩坑 E**：师兄的 `~/zynq-deploy.sh` **Step 2 缺 sshpass**（他本地有 SSH agent 所以没暴露），SSH 我这边跑挂
- **踩坑 F**：Petalinux 中继机的老 OpenSSH 不认识 `PubkeyAcceptedAlgorithms`（8.5+ 才有），带这个 `-o` 参数崩
- 绕道：**Windows 直接 scp exe 到板子**（W2 同款路径），跳过 relay
- **踩坑 G**：dropbear scp `-r` 到不存在目录会 canonicalization fail，先 `mkdir /tmp/nuclides` 再 scp
- 把 exe + nuclides/ 挪到 `/home/root/simmax/` 持久位置（/tmp 是 tmpfs）
- 首次启动：**PID 存活，日志出现"加载核素 3 个"**，framebuffer /dev/fb0 有内容写入

### P4: 显示适配（03:15 ~ 03:50）

**目标**：让 Qt 面板中文正常 + 全屏正确 + 开机自启。

**踩坑 H：字体缺失**
- 板子只有 `/lib/DejaVuSans.ttf`（不含 CJK），中文全部方框（tofu）
- 板子**没有 `fc-cache` 命令**，无法重建 fontconfig 缓存
- 解决：`main.cpp` 里用 `QFontDatabase::addApplicationFont("simhei.ttf")` 加载 exe 同目录字体，设为 QApplication 默认字体，**绕开 fontconfig**
- scp Windows 的 `C:\Windows\Fonts\simhei.ttf` (9.5MB) 到板子 `/home/root/simmax/`
- 日志出现 `family: "SimHei"` ✅，中文正常显示

**踩坑 I：`setFixedSize()` 被 Qt 5.9 linuxfb 忽略**
- fb0 virtual_size = 1280×1440（double buffer），`showFullScreen()` 把窗口撑到 QSize(1280, 836)
- 底部 116px 超出屏幕（720），statusBar/AXI 寄存器/波形 X 轴刻度全被裁
- 尝试 `setWindowFlags(Qt::FramelessWindowHint) + setFixedSize(1024, 600)` **无效**，Qt 5.9 linuxfb 强制窗口 = fb 大小
- **正解**：`QT_QPA_PLATFORM='linuxfb:size=1024x600:offset=0,0'` 平台参数从**平台层**限定虚拟屏
- 加了诊断 `qInfo() << MainWindow.size() << waveform.size()...` 精准调试

**踩坑 J：Linux 无法覆盖运行中的 exe (ETXTBSY)**
- `scp` 覆盖 `/home/root/simmax/simmax_panel` 时报 `Failure`
- 修复：**先 stop → scp → start** 三步走
- 教训：deploy 脚本必须内置这个顺序（师兄脚本没做）

**踩坑 K：qmake diagnostics 需要显式 include**
- 加 `qInfo() << ...` 报 `class QDebug forward declaration` → 补 `#include <QDebug>`
- 用 `menuBar()` 报 `class QMenuBar forward declaration` → 补 `#include <QMenuBar>`
- QMainWindow.h 只 forward declare，用要显式 include

**成品**：
- Qt 面板 1024×600 从**左上角**显示（模拟 7 寸屏效果，便携屏其余为黑边）
- Splitter 波形:参数 = 7:3（717:307）
- SimHei 中文字体正常渲染
- `/etc/init.d/simmax` + `/etc/rc5.d/S99simmax` **开机自启**（bitstream 加载 + Qt 启动一条龙）
- **`/sbin/reboot` 后一次通过**：`up 0 min` → `simmax running (PID=1120)` → 屏显示 1024×600 面板

### 关键技术链路（今日全流程）

```
[Windows Vivado]
  └── C1 全脚本 TCL → BD 综合 + P&R (WNS +1.1ns) → write_bitstream (.bit + .bin)

[Python 本地 4-byte swap]  ← 坑 B (fpga_manager 要 byte-swapped)

[Windows scp + -o +ssh-rsa]
  └── BX71 板 /lib/firmware/tier3_top.bin
      → echo > /sys/class/fpga_manager/fpga0/firmware
      → state=operating ✅ PL 烧入 tier3_top

[Windows scp → 师兄 WSL]
  └── ~/simmax_qt/ (21 files)
      → qmake -spec linux-arm-gnueabihf-g++ → make -j16
      → simmax_panel (143 KB ARM ELF)

[Windows 直连板子 scp]
  └── /home/root/simmax/simmax_panel + simhei.ttf + nuclides/
      → /etc/init.d/simmax start
      → QT_QPA_PLATFORM='linuxfb:size=1024x600:offset=0,0'
      → 1024×600 Qt 面板显示 (左上角, HDMI 720P 其余黑边)
```

### 今日总踩坑数：**11 个** (A-K)

比 C1 阶段本身还多，主要集中在跨平台 + Qt embedded 陷阱。这些坑值得整理进 `fpga-rules.md` 后半段 "Qt/linuxfb 篇" 或新建 `qt-embedded-rules.md`（下次归档时做）。

### 下一步（明天以后）

- **颜色 RGB565 R/B 互换**（波形现在是蓝色，本应绿色）— 写 `swap_rb()` helper 全项目替换 QColor
- **AXI 寄存器 debug 面板**在 1024×600 是否完整显示 — 观察后决定压缩或删除
- **接 devmem/mmap 真 AXI 读写**（师兄 rootfs 加 devmem 工具，或自己写 mmap /dev/mem 的 C helper）
- **师兄完整 rootfs**（含 gcc + devmem + rsync）— 长期规划
- 主项目 git 仓库 `xinman-2026-nuclear-pulse-gen` 择日建（今日仍暂缓）
