# 工作日志 - 2026年7月28日

（实际时间：无法从转录文件获取精确时段）

## [新漫杯] Qt 5.15.2 + MinGW 开发环境搭建完成

### 环境诊断与修复过程

Qt Creator 20.0.0 空壳安装后，通过 aqt 补装了 Qt 5.15.2 库（`D:\Qt\5.15.2\mingw81_64\`）和 MinGW 工具链。但在实际构建时遇到一系列连锁问题：

**问题 1：Kit 用了 MSVC 编译器**
- 最初用 CMakeLists.txt，Kit "桌面" 默认选了 Visual Studio 2022 BuildTools 的 MSVC 编译器（`cl.exe`），而不是 MinGW
- 编译报错：找不到 Qt5 cmake 配置文件，`CMAKE_PREFIX_PATH` 和 `QT_QMAKE_EXECUTABLE` 为空
- 解决：改用 Qt5 原生 `.pro` 工程文件（qmake），并确认 Kit 选 "Qt 5.15.2 MinGw"

**问题 2：aqt 安装的 MinGW 是空的**
- `D:\Qt\Tools\ming1120_64\` 目录存在但为空 —— aqt install-tool 静默失败
- Qt Creator 的 toolchains.xml 中 ToolChain.17/18 指向这个空路径的 `gcc.exe`/`g++.exe`
- 编译器不存在 → 锤子灰色不可点

**问题 3：找到系统已有的 GCC —— WinLibs GCC 16.1.0**
- 系统通过 WinGet 安装了 WinLibs MinGW：`C:\Users\29035\AppData\Local\Microsoft\WinGet\Packages\BrechtSanders.WinLibs.POSIX.UCRT_Microsoft.Winget.Source_8wekyb3d8bbwe\mingw64\bin\`
- 真实 GCC 16.1.0（不是 clang 包装器）
- 手动修改 `toolchains.xml`，将两条编译器路径从空 `ming1120_64` 改为 WinLibs 的真实 GCC

**问题 4（根因）：LLVM-MinGW clang 伪装成 gcc/g++ 抢占 PATH**
- LLVM-MinGW（也是 WinGet 安装的）在 PATH 中排在 WinLibs 之前
- 其 `gcc.exe`/`g++.exe` 实际是 clang 包装器，不支持 GCC 特有选项 `-fno-keep-inline-dllexport`
- qmake 调用 `g++` 时找到的是 clang → 报 `unknown argument: '-fno-keep-inline-dllexport'`
- 即使手动 `export PATH=WinLibs:...` 也无法解决（shell 缓存 + PATH 顺序问题）
- **最终解决**：重命名 LLVM-MinGW 的 `gcc.exe`/`g++.exe` → `gcc-clang-wrapper.exe`/`g++-clang-wrapper.exe`，让真正的 GCC 成为默认

### 验证结果
- 命令行 `qmake test.pro && mingw32-make` → 编译成功，生成 `release/test.exe`
- 只有 Qt 5.15.2 头文件的 deprecated enum 警告，无 error

### 关键文件修改
| 文件 | 修改 |
|------|------|
| `toolchains.xml` | ToolChain.17/18 的编译器路径：`D:/Qt/Tools/ming1120_64/bin/gcc(g++).exe` → WinLibs GCC |
| LLVM-MinGW bin/ | 重命名 `gcc.exe`/`g++.exe` → `gcc-clang-wrapper.exe`/`g++-clang-wrapper.exe` |
| `test.pro` | 新建 Qt5 qmake 工程文件，替代 CMakeLists.txt |

### 环境清单（终态）
| 组件 | 路径 | 版本 |
|------|------|------|
| Qt 库 | `D:\Qt\5.15.2\mingw81_64\` | 5.15.2 |
| qmake | `D:\Qt\5.15.2\mingw81_64\bin\qmake.exe` | Qt 5.15.2 |
| C/C++ 编译器 | WinLibs `mingw64\bin\gcc.exe` / `g++.exe` | GCC 16.1.0 (MinGW-W64) |
| Qt Creator | （开始菜单） | 20.0.0 |

---

---

## Qt Creator 内编译最终突破（续）

### 问题 5（后续新发现）：两个 Kit 同时绑定 Qt 5.15.2
- profiles.xml 中存在两个 Kit 都关联了 Qt 5.15.2：
  - `Desktop (x86-windows-msys-pe-64bit)` — 使用 clang x86_64 工具链
  - `Qt 5.15.2 MinGw` — 使用 WinLibs GCC 工具链
- 打开 test.pro 时配置窗口两个都显示，clang Kit 排在前面被优先选中
- clang 不支持 GCC 专属选项，报 `--target=x86_64-w64-mingw32` → 四个编译错误

### 问题 6（踩坑教训）：不能边运行 Qt Creator 边改它的 XML 配置
- 尝试了多次直接编辑 profiles.xml / toolchains.xml
- **Qt Creator 每次运行时会自动覆写这些文件**，手工修改会被丢弃或导致配置混乱
- 删除 Profile.3 后 Qt Creator 甚至删除了"Qt 5.15.2 MinGw"永久 Kit，只留下 3 个不相关的 Kit
- **教训：以后配置 Qt Creator 一律走它自己的 Preferences 界面，不要改 XML**

### 最终解决方案
- Qt Creator 自动生成了临时 Kit：**"Qt 5.15.2(mingw81_64)-临时"**
- 该临时 Kit 关联了正确的 Qt 5.15.2 qmake + WinLibs GCC 编译器
- 之前的三个底层修复（重命名 clang wrapper、mkspec 绝对路径、toolchains 指向 WinLibs）全部生效
- **编译成功，无错误**

### 编译链路（终态）
```
Qt Creator 20.0.0
  → 临时 Kit "Qt 5.15.2(mingw81_64)-临时"
    → Qt 5.15.2 qmake (D:/Qt/5.15.2/mingw81_64/bin/qmake.exe)
      → mkspec win32-g++/qmake.conf（GCC 绝对路径：WinLibs GCC 16.1.0）
        → g++/gcc 直接调用 WinLibs（绕过 PATH）
          → 编译成功 ✓
```

### 所有修复项汇总

| # | 修复 | 层级 |
|---|------|------|
| 1 | 重命名 LLVM-MinGW 的 `gcc.exe`/`g++.exe` → wrapper | 系统 PATH |
| 2 | 修改 `toolchains.xml`：MinGW 工具链路径 → WinLibs GCC | Qt Creator 编译器注册 |
| 3 | 修改 `qmake.conf`：`gcc`/`g++` → WinLibs GCC 绝对路径 | qmake mkspec |
| 4 | 删除 clang Kit 的 Qt 关联（禁用它匹配 test.pro） | Qt Creator Kit 管理 |
| 5 | Qt Creator 自动生成临时 Kit → 所有修复串联生效 | Qt Creator 自动恢复 |

---

---

## 运行时 DLL ABI 冲突修复

### 问题 7：`无法定位程序输入点 _ZSt21_glibcxx_assert_fail` 
- 编译通过，运行时报错：test.exe 无法定位 libstdc++ 符号
- **根因**：Qt 5.15.2 DLL 用 GCC 8.1.0 的 libstdc++ 编译，但 WinLibs GCC 16.1.0 的 libstdc++-6.dll 版本不兼容
- 运行时加载了新版 libstdc++ DLL，Qt DLL 里的符号找不到
- **解决**：在 `qmake.conf` 增加 `-static-libstdc++ -static-libgcc`，把 libstdc++ 静态编译进 exe

### 最终验证
- Qt Creator 内编译 + 运行成功
- 弹出 800×600 空白 MainWindow 窗口
- **Qt 5.15.2 + MinGW GCC 16.1.0 全链路打通** ✓

---

**产出**：
- `D:\Qt_Project\test\test.pro`
- `D:\Qt_Project\test\release\test.exe`（命令行编译产物）
- Qt Creator 内编译 + 运行通过 ✓
