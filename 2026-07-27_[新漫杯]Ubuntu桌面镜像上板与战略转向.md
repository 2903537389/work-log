# 工作日志 - 2026年7月27日

（实际时间：约 07-27 09:00 ~ 16:48 北京时间，从对话转录文件最后一条 timestamp `2026-07-27T08:48:07.129Z` 推算为北京时间 16:48）

## [新漫杯] Ubuntu 桌面镜像上板 + Qt 环境诊断 + 战略转向

### Ubuntu 桌面镜像烧录与启动
昨天决定换成 Ubuntu 桌面镜像，今天实际烧录测试：

- 文件：`D:\FPGA学习资料\BX71\应用系统\Ubuntu桌面镜像\BX71_7010_and_7020_Ubuntu.zip`
- 解压到 `E:\ubuntu_img\BX71_7010_and_7020_Ubuntu.img`（约 8GB）
- Win32DiskImager 写入 SD 卡

**上板结果**：
- 7" IPS 屏幕：闪烁无内容（分辨率不兼容——Ubuntu 默认 1080P，7 寸屏只支持 1024×600）
- **接正经 PC HDMI 显示器**：正常显示 Ubuntu 16.04.4 LTS 桌面 ✅

### 板上环境诊断（以 zynq 账户自动登录桌面）

| 项目 | 状态 |
|---|---|
| 内核 | Linux 4.14.0-xilinx armv7l |
| 发行版 | Ubuntu 16.04.4 LTS (ID_LIKE=debian) |
| 默认用户 | zynq（自动登录） |
| gcc | `/usr/bin/gcc` ✅ |
| qmake | `/usr/bin/qmake`（**空壳**——qtchooser 指向不存在的 Qt4 路径） |
| Qt5 运行时库 | core/gui/widgets/svg/script 等全装 ✅ |
| Qt5 开发工具 | **完全没装** ❌（find 找不到任何 qmake-qt5） |
| `/lib/modules/` | **不存在**——Xilinx static kernel ❌ |
| 网口 | eth0 物理层 OK（LOWER_UP）但无 YT8531 驱动 |

**结论**：Ubuntu 桌面镜像只是一个"演示预装 Qt5 应用的镜像"，不是开发环境。板上能跑现成的 Qt 程序，但**不能在板上编译新的 Qt 程序**。

### 尝试联网装 Qt5 dev（全部失败）

**路径 1：PC ICS 共享网络**
- Windows 11 开网络共享（WLAN 共享给以太网）
- PC 以太网 IP 自动变为 192.168.137.1
- 板子手动配 eth0 192.168.137.100/24 + 网关 192.168.137.1
- **双向 ping 不通**——与 07-26 同症状（复现）
- 疑似 Hyper-V vSwitch 虚拟网卡（WSL2 安装带来的）劫持 ARP

**路径 2：iQOO 手机 USB 网络共享**
- lsusb 识别到手机 VID=2d95、PID=600a（vivo/iQOO）
- 但未创建 usb0 网卡
- modprobe rndis_host → 报错 "can't open moddep"（无 /lib/modules/）
- 手动 `echo "2d95 600a" > /sys/bus/usb/drivers/rndis_host/new_id` 也无效（驱动非模块形式）
- **分析**：rndis_host 驱动可能已编入内核，但没有为 iQOO vivo 的 VID 做自动匹配

### YT8531 网口问题深入排查
通过 `D:\simmax\DAC以及7010资料\` 下的源码资料：
- 从 Vivado Block Design (`Sys.bd`) 提取：GEM0 → EMIO → gmii_to_rgmii 桥（C_PHYADDR=31）
- 设备树 `system-user.dtsi`：PHY reg=<0x5>
- PHY addr 不一致（31 vs 5）——但这是 V3（RTL8211F）遗留的次要问题
- **根因**：GMAC（原厂被收购后的中国子公司）提供的 YT8531 **专用驱动源码**（`motorcomm.c`）已在小梅哥资料中找到（`D:\simmax\DAC以及7010资料\YT8531\` 目录）
- Generic PHY 驱动无法初始化 YT8531 的 RGMII 时钟延迟/输出模式 → GEM 报 `unable to generate target frequency: 125000000 Hz` → link flapping

YT8531 驱动源码已作为交付物转给师兄。

### 战略转向决定

**放弃在残缺小梅哥镜像上折腾**。新分工：
- **师兄**：编译完整 PetaLinux 内核（含 YT8531 YT8531 驱动 + g_ether gadget + Qt5-dev + gcc + SSH）
- **我**：PC 端 Qt Creator 5.15 桌面开发（先在 PC 跑通 GUI 原型，等师兄 SDK 后交叉编译）

**交付物**：
- `D:\simmax\_师兄内核编译需求清单.md`（完整需求文档）
- YT8531 驱动源码路径（供师兄编译用）

### USB A to A 线确认
小梅哥随板**送了一根 USB A to A 线**——证实 BX71 USB0 支持 Device 模式可做 g_ether gadget。师兄编好内核后，部署阶段用这根线 + g_ether 传 Qt 程序。

### Qt 安装尝试（未完成）
Qt 官网 Online Installer 现在只给 Qt 6.11 商业试用版，不提供 Qt 5.15.2 开源选项。离线包 `qt-opensource-windows-x86-5.15.2.exe` 从 `https://download.qt.io/archive/qt/5.15/5.15.2/` 或清华镜像获取。本小姐给了迂回方案。

---

**产出**：
- `D:\simmax\_师兄内核编译需求清单.md`
- `C:\Users\29035\Desktop\shell_test.ps1`（串口唤醒脚本）
- `C:\Users\29035\Desktop\qt_test\`（板上 Qt Hello World 程序，含 main.cpp + qt_test.pro）

**遗留问题**：
- PC↔BX71 直连网线 ARP 不通（待师兄内核 + USB A to A 方案替代）
- 7" IPS 屏 Ubuntu 桌面分辨率不兼容（待师兄内核修改默认分辨率）
- Obsidian Wiki 有本地 commit 因网络问题未推送
