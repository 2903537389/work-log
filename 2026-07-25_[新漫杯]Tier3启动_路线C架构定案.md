# 工作日志 - 2026年7月25日

## [新漫杯] Tier 3 启动 · 路线 C (Linux + Qt5) 架构定案

**竞赛：** 2026 新漫杯全国大学生核仪器创新大赛
**项目：** 智能仿核信号发生器
**队伍：** 写的代码全队
**平台：** BX71 XC7Z010 (ZYNQ) + ACM9767 DAC + 7" IPS 电容触摸屏 + EC11 旋钮
**Vivado 版本：** 2018.3

---

## 一、今日做了什么（实际时间：07-25 15:28 ~ 16:33）

Tier 2 上板全部通过后（幅度精度实测 ±0.6%），今天进入 Tier 3 架构讨论。核心成果：**放弃 A→B→C 渐进策略，直接一步到位路线 C（Linux + Qt5 全功能仪器）**。完成 AXI-Lite 寄存器映射设计（24 个寄存器），扩展 `axi_waveform_ctrl.v` 到 v0.2 完整版，产出 Tier 3 开发指引文档。

## 二、赛题深度分析（实际时间：07-25 15:30 ~ 15:50）

读了原始赛题《附件2》完整版本，提取 11 个需要"可调"的参数：脉冲幅度 / FWHM / 周期 / 噪声 RMS / τ₁ / τ₂ / 波形模式 / 输出模式 / 能谱数据 / 核素选择 / 平均计数率 λ。

赛题两处"或"字给了策略空间：
- "需具备显示屏 **或** 上位机" → 双通道加分
- "按键 **或** 上位机软件设置" → EC11 替代按键

赛题所说的"单片机内部..." 在我的平台就是 ZYNQ PS (ARM Cortex-A9)，天然需要启用 PS 端才能完成高级功能 25 分。

## 三、Tier 3 三条路线对比与决策（实际时间：07-25 15:50 ~ 16:15）

原本推荐 A→B→C 渐进：
- **A. 纯 PL 独立仪器** (~3-5 天)：能显示、能上位机、不能触摸、脱机不能跑高级
- **B. PS bare-metal + AXI** (~7-10 天)：脱机全功能、触摸屏不能摸
- **C. Linux + Qt5** (~12-18 天)：一步到位但学习曲线陡

我选择方案 A→B→C 渐进推进，因为想"先做出能用能显示的东西"。但对话中意识到 A 阶段的 PL 简单 HDMI 会被 C 阶段的 Qt HDMI 完全替换，产生重写。

**最终决策：直接一步到位路线 C**。虽然中间可能 15-19 天看不到屏幕，但避免了 HDMI 层重写。

关键决策：

| 决策项 | 结论 |
|---|---|
| 路线 | **C：Linux + Qt5 全功能仪器** |
| GUI（ZYNQ 侧）| Qt5 (C++) |
| GUI（PC 上位机）| **Qt5 (C++)** — 跟 ZYNQ 侧统一，代码可复用 |
| HDMI 驱动 | PS framebuffer → PL VDMA + TMDS |
| 触摸屏 | 慧勤 7" IPS 电容触摸 ¥248（HDMI + USB）已到货 |
| EC11 | PS GPIO |
| Linux | PetaLinux 2018.3 |
| Ubuntu 平台 | VMware 虚拟机装在 E 盘 |
| BX71 BSP | 暂时不管，先搞 Ubuntu 环境 |

## 四、AXI-Lite 寄存器映射设计（实际时间：07-25 16:15 ~ 16:25）

设计 24 个寄存器覆盖全部 Tier 3 功能，地址空间 256 字节（8bit ADDR_WIDTH）：

| 分组 | 偏移范围 | 数量 | 状态 |
|---|---|---|---|
| §1 基础参数（Tier 2 已用）| 0x00-0x24 | 10 | ✅ 硬件已实现 |
| §2 泊松间隔（Tier 3.7）| 0x28-0x34 | 4 | ⏳ 寄存器就绪，硬件待接 |
| §3 能谱采样（Tier 3.6）| 0x38-0x4C | 6 | ⏳ 寄存器就绪 |
| §4 核素快捷 | 0x50-0x54 | 2 | ⏳ 寄存器就绪 |
| §5 预留 | 0x58-0xFC | ~40 | 预留 |

**关键设计**：能谱采样采用"PS 算 sample 推给 PL"模式：
- Qt 应用启动时把能谱 CDF 表算好
- PL 需要下一个脉冲时置 `spectrum_trig`
- PS 生成均匀随机数 → 二分查找 CDF → 幅度写入 `CH1_SAMPLE_AMPL`
- PL 消耗后置 `sample_ack`
- 最大重频 ~10 kHz 无压力

完整映射文档：`D:\.newmam_project_all\docs\axi_register_map.md`

## 五、代码产物（实际时间：07-25 16:25 ~ 16:33）

**新写文件**：

| 文件 | 说明 | 行数 |
|---|---|---|
| `axi_waveform_ctrl.v` (v0.2) | AXI-Lite Slave 24 寄存器完整版 | 305 |
| `tier3_top.v` | Tier 3 顶层框架（含 PS7 + AXI 接口占位）| 175 |
| `docs/axi_register_map.md` | 24 寄存器映射唯一真相源 | 150 |
| `scripts/setup_tier3_bd.tcl` | Vivado Block Design 自动搭建脚本 | 90 |
| `D:\simmax\_Tier3开发指引_必读.md` | **切换模型必读的当前工作指引** | 120 |

**改动文件**：

| 文件 | 改动 |
|---|---|
| `pl_top.v` | 添加 `USE_AXI_PARAMS` 参数，`=0` 时用 localparam，`=1` 时用 AXI 输入 |

**关键设计原则**：MVP 和升级方案都能直接用同一份代码，不需要重写。

## 六、开发环境规划

### 第一次决策：VMware 虚拟机（07-25 16:33）

初次选 VMware Workstation Player 方案，理由：不动主分区、USB 直通简单、快照回滚。

### 第二次决策：改为 WSL2（07-25 16:45）

用户反馈 VMware 不好用（启动慢 + 占内存 + 文件跨系统麻烦），改用 WSL2 方案。

WSL2 相对 VMware 的优势：
- **秒级启动**（无 GUI 冷启动）
- **按需分配内存**（不用固定 8GB）
- **文件互通天然**（`\\wsl$\Ubuntu-18.04\` 直接访问）
- Qt 开发 IDE 放 Windows 侧（Qt Creator + Designer），WSL 只做 PetaLinux 编译
- USB 直通用 `usbipd-win`（微软官方开源工具）

WSL2 装 PetaLinux 2018.3 的关键坑：
1. **编译必须在 WSL 原生文件系统**（`~/petalinux/`），不能在 `/mnt/e/`
2. **WSL 虚拟磁盘迁到 E 盘**（`wsl --export/import`）
3. **usbipd-win** 配置 BX71 USB-JTAG 直通
4. `TERM=xterm-256color` 环境变量（否则编译某些交互界面报错）
5. Ubuntu 18.04 rootfs 需从 Ubuntu 云镜像下载（微软商店已不上架）

**下一步用户自主操作**：
1. PowerShell 管理员运行 `wsl --install` → 重启
2. 下载 Ubuntu 18.04 cloud rootfs → 用 `wsl --import` 装到 E 盘
3. 换阿里源 + 建普通用户 + 装 PetaLinux 依赖包 + 设 locale
4. Windows 侧装 usbipd-win，验证 BX71 能 attach 到 WSL
5. 装完后通知本小姐，进入 PetaLinux 2018.3 下载安装

## 七、今日新增 FPGA 知识

无新增坑记录（本轮为架构决策为主）。三个 FPGA 坑（DDS wrap around / 单指数静默期 / ACM9767 双极性）已在 07-24 归档。

## 八、Git 归档

- work-log 仓库 → 提交本文
- Obsidian Wiki 仓库 → session-archive 追加 07-25 条目
- 主项目 `D:\.newmam_project_all` → 仍未建 git 仓库（等 Tier 3 完整验证后一起建）

## 九、下一步路径

按 Tier 3 阶段分解：

| 阶段 | 内容 | 状态 |
|---|---|---|
| C1 | Block Design（PS7 + AXI-Lite） | 🚧 代码已备，待 Vivado 搭建 |
| **C2** | **Ubuntu 虚拟机 + PetaLinux 安装** | **⏳ 用户自主安装中** |
| C3 | HDMI 显示管线（VDMA） | 待 C1/C2 完成 |
| C4 | Qt5 交叉编译 + 基础 GUI | 待 C2/C3 完成 |
| C5 | EC11 参数交互 | 待 C4 完成 |
| C6 | 能谱引擎 + 核素数据库 | 待 C4 完成 |
| C7 | 泊松间隔 + PC Qt C++ 上位机 | 待 C4 完成 |
| C8 | 联调 + 外壳 + 答辩 | 待前面全部完成 |

**开发风格约定**：用户不按天数管理进度，本小姐按里程碑推进，checkpoint 至关重要。

---

## 十、WSL2 + PetaLinux 环境搭建（实际时间：07-25 16:45 ~ 21:30）

### 10.1 放弃 VMware，改 WSL2
VMware 启动慢、占 8GB 固定内存、文件跨系统不便。改用 WSL2 方案：Qt Creator 在 Windows 侧开发，WSL 只做 PetaLinux 编译。

### 10.2 踩坑记录

**坑 1（WSL 环境）**：CPU 虚拟化未开启
- 现象：`wsl --status` 报 "WSL2 无法启动，因为此计算机上未启用虚拟化"
- 修复：进 BIOS → Advanced → Intel Virtualization Technology → Enabled
- 同时需在 Windows 功能里启用"虚拟机平台"（`wsl --install --no-distribution`）

**坑 2（Ubuntu 18.04 rootfs）**：微软商店不再上架 18.04
- 从清华镜像下载 `bionic-server-cloudimg-amd64-wsl.rootfs.tar.gz`（319MB）
- 用 `wsl --import Ubuntu-18.04 E:\WSL\Ubuntu-18.04 rootfs.tar.gz --version 2` 导入
- 注意：`wsl --install` 默认装的是 22.04/24.04，需手动卸载

**坑 3（PetaLinux 依赖缺失）**：首次安装报缺 5 个包
- 缺：diffstat、chrpath、socat、unzip、zlib1g:i386
- zlib1g:i386 需要先 `dpkg --add-architecture i386` 再 `apt update`

**坑 4（PetaLinux `command not found`）**：PetaLinux 2018.3 **没有主 petalinux 命令**
- 所有功能通过子命令直接调用：`petalinux-create`、`petalinux-build`、`petalinux-config` 等
- `source settings.sh` 后 PATH 已正确设置，子命令全能用 ✅

**坑 5（PetaLinux 工程路径）**：工程**不能放在 /mnt/e/ 下！
- 原因：bitbake 需要 Linux 原生文件系统的符号链接支持，Windows 文件系统（/mnt/ 挂载）不支持
- 修复：工程放在 WSL 内部 `~/petalinux_projects/` 下

**坑 6（bitbake source 失败）**：SDK 文件路径不匹配
- 现象：`environment-setup-cortexa9hf-neon-xilinx-linux-gnueabi: No such file or directory`
- 根因：script 期望 `/source/arm/` 子目录，但 SDK 把所有文件解压在 `/source/` 根下
- 修复：`mkdir arm && ln -s ../environment-setup-* arm/` 创建符号链接

**坑 7（多余发行版）**：WSL 里意外多了个 Ubuntu（22.04/24.04）
- 修复：`wsl --unregister Ubuntu` 删除

### 10.3 环境验证清单

| 项 | 状态 |
|---|---|
| WSL2 + Ubuntu 18.04.6 LTS | ✅ 装在 E 盘 |
| PetaLinux 2018.3 工具链 | ✅ 子命令全通 |
| /bin/sh → bash | ✅ 已切换 |
| PetaLinux 工程 tier3_linux | ✅ 已建，HDF 已导入 |
| bitbake 1.36.0 | ✅ 可用 |
| source settings.sh 自动加载 | ✅ .bashrc 已配 |

### 10.4 预编译镜像验证失败（ZCU106 ≠ Zynq-7000）
用 `file` 命令检查了 E:\petalinux\原始image\ 里的 BOOT.BIN：
- `Zynq UltraScale+ MPSoC, 64-bit` → 是给 ZCU106 用的 ❌
- BX71 是 `XC7Z010（Zynq-7000, 32-bit Cortex-A9）`，架构不兼容
- Factory_Linux 工程里的 BOOT.BIN 才是对的（`Zynq 7000 SoC, 32-bit`）✅
- **结论**：必须自己编译 PetaLinux，不能靠预编译镜像

### 10.5 下一步
- `petalinux-build` 完整编译（首次 2-4 小时，明天跑）
