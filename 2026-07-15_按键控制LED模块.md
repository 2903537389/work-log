# 工作日志 - 2026年7月15日

## 按键控制 LED 模块开发（实际时间：7月15日凌晨）

### 完成事项

- **设计并编写 `led_button.v`**：包含消抖 + 边沿检测 + LED 状态机的完整模块
- **编写 `led_button_tb.v`**：模拟多次按键操作的仿真 Testbench
- **创建独立的工程文件夹** `C:/XiLinxProject/led_button_src/`，与流水灯工程分离
- **发现并修复非阻塞赋值经典 bug**：`<=` 同拍内右边的 `stat` 读的是旧值，导致 LED 显示慢一拍

### 技术要点

#### 模块架构

```
M5引脚 → key_in → [消抖计数器 20ms] → key_stable → [边沿检测] → key_pressed → [LED状态机] → led输出
```

**三个 always 块并行运行**：
1. 消抖：`key_in != key_prev` 清零计数器，稳定 20ms 后锁存 `key_stable`
2. 边沿检测：`key_stable_prev && !key_stable` = 下降沿脉冲（1 个时钟周期宽）
3. LED 状态机：`key_pressed` 触发 → `stat+1` → `led = ~(1 << next_stat)`

#### 非阻塞赋值 Bug 修复

**原代码（有 bug）**：
```verilog
stat <= stat + 1;
led  <= ~(1 << stat);    // Bug: stat 是旧值！
```

**根因**：`<=` 非阻塞赋值在同一个 `posedge clk` 里，所有右边的表达式读到的都是"拍开始时的旧值"。第 1 行 `stat <= stat + 1` 到下一拍才生效，第 2 行读到的是未更新的 `stat`。结果：第一次按下 LED 不变化，第二次按才显示第一次的结果。

**修复方案（方案二：next_stat 线）**：
```verilog
wire [1:0] next_stat;
assign next_stat = stat + 1;      // 组合逻辑预计算

if (key_pressed) begin
    stat <= next_stat;
    led  <= ~(1 << next_stat);    // 两个 <= 右边读同一个值，不会错位
end
```

#### Verilog 语法学习

| 概念 | 说明 |
|------|------|
| `reg [1:0] stat` | 2 位寄存器，自动回绕（3+1=0）|
| `assign next_stat = stat + 1` | 组合逻辑连线，输入变输出立刻变 |
| `wire [1:0] next_stat` | 声明一根 2 位宽的总线 |
| `<=` 非阻塞赋值 | clk 沿同时读旧值、同时写新值 |
| `=` 阻塞赋值 | 立刻生效，用于 `assign` 和组合 `always @(*)` |

### 工程文件
- `C:/XiLinxProject/led_button_src/led_button.v` — 按键控制 LED 模块（方案二 next_stat 版）
- `C:/XiLinxProject/led_button_src/led_button_tb.v` — Testbench
- `C:/XiLinxProject/build_led_button.tcl` — 一键建工程+启动仿真

---

## CLAUDE.md 配置文件拆分（实际时间：7月15日凌晨）

将原来 975 行的单一 `~/.claude/CLAUDE.md` 拆分为 3 个文件：

| 文件 | 内容 |
|------|------|
| `~/.claude/CLAUDE.md` | 通用规则（中文回复、工作日志、归档、自动加载子文件指令）|
| `~/.claude/stm32-esp32-rules.md` | STM32/ESP32 嵌入式 C 驱动开发规则 |
| `~/.claude/fpga-rules.md` | FPGA/Verilog/Vivado 开发规则（新创建）|

CLAUDE.md 开头加了自动加载指令，每次对话开始时自动读取两个子规则文件，确保 STM32/ESP32/FPGA 能力在所有工作目录下都可用。

### 下一步
- 上板测试：添加 key_in(M5) 的 XDC 约束，综合+烧录，拨动拨码开关验证
- 继续学习路线：DDS 波形引擎
