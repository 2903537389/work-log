# 工作日志 - 2026年7月14日

## FPGA仿真全流程学习（实际时间：7月14日 凌晨 04:00~05:30）

### 今日目标
学习 Vivado 行为仿真（Behavioral Simulation）的完整操作流程，掌握 Testbench 的使用方式，为后续 FPGA 调试打下基础。

### 完成的里程碑
- **打通 Vivado 仿真全流程**：启动仿真 → 添加信号 → run → 缩放波形 → 截图 → close_sim，整个循环走通
- **成功看到 LED 波形跳变**：在仿真波形中观察到 clk 方波、rst 复位释放、count 递增、LED 状态切换
- **掌握波形查看技巧**：Ctrl+滚轮缩放时间轴（向上放大、向下缩小），Ctrl+中键拖拽平移

### 技术要点

#### Vivado 仿真操作核心流程（5 步循环）
1. Flow Navigator → Simulation → Run Behavioral Simulation
2. Scope 面板点选被测模块 → Objects 面板 Ctrl+A 全选信号 → 拖入波形窗口
3. Tcl Console 输入 `run <时间>`
4. 波形区 Ctrl+滚轮放大查看细节，截图记录
5. `close_sim -force` 关闭仿真

#### 关键教训：仿真必须停止

**事故**：跑完仿真后没关波形窗口出去吃饭，回来后 C 盘被 Vivado 仿真缓存塞爆。

**根因**：Vivado GUI 仿真波形数据写入 `%TEMP%` 临时目录，如果 Testbench 里没有 `$finish` 或者长时间 run，缓存会无限增长。

**三条铁律**：
1. Testbench 里必须写 `$finish`（或 `$stop`），有明确终止时间
2. GUI 仿真看完波形立刻 `close_sim`，不要挂着窗口离开
3. 长仿真（毫秒级以上）用 Tcl 命令行 `run all` 模式

**事后清理**：`C:\Users\29035\AppData\Local\Temp\` 下 `vivado_*` 开头的文件夹

#### 遇到的 UI 问题和解决

| 问题 | 原因 | 解决 |
|------|------|------|
| Scope 面板只有 `glbl` 和 `led`，没有 `uut` | Vivado 在 Scope 里显示的是**模块类型名**而不是实例名；`led` 就是被测模块 | 点 `led` 即可，Objects 会列出所有内部信号 |
| 右键 Set as Top 灰色 | `led_tb.v` 被加到了 Design Sources 而不是 Simulation Sources | 从 Design Sources 移除 → 通过 Add Sources 加到 Simulation Sources |
| 波形窗口被误删 | 关掉了 untitled 波形标签页 | 顶部菜单 Window → Waveforms 恢复 |
| clk/rst 全是 Z，其余是 X | 仿真还没 run，信号未初始化 | run 500ns 后即有波形 |

#### LED 流水灯仿真优化

**问题**：原代码 count 阈值 = 10,000,000（0.2s），仿真里需要跑 200ms 才能看到一次 LED 切换，波形文件会巨大。

**解决方案**：在 `led.v` 中加入 parameter `COUNT_MAX`：
- 仿真时 Testbench 覆写为 `COUNT_MAX = 4`（5 个时钟周期 = 100ns 切换一次）
- 上板时使用默认值 10M（保持 0.2s 间隔）

```verilog
// led.v
module led #(
    parameter COUNT_MAX = 10000000 - 1
) (...)

// led_tb.v
led #(.COUNT_MAX(5 - 1)) uut (...)
```

**仿真结果**（500ns）：clk 为 50MHz 方波（20ns 周期），count 每周期 +1，到 4 时清零并切换 LED 状态（F → E → D → ...）。

### 工程文件
- `C:\XiLinxProject\EX08_Led_Flow_src\led.v` — 加入 parameter COUNT_MAX
- `C:\XiLinxProject\EX08_Led_Flow_src\led_tb.v` — 覆写 COUNT_MAX=4，加 $finish 警示注释
- `C:\Users\29035\Desktop\新漫赛题\FPGA工程师视角_项目知识索引.md` — §9.1 新增仿真核心禁忌（C 盘爆炸事故+三条铁律+清理路径），§10 新增会诊条目

### 下一步
- 设计按键控制 LED 的 Verilog 模块（练手：输入信号处理、消抖逻辑）
- 继续推进学习路线：改代码练手 → DDS 波形引擎
