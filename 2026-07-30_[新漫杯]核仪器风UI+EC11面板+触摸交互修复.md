# 工作日志 - 2026年7月30日

## 概览

从昨晚 (07-29 22:30 左右) 到今晨 (07-30 02:10)，约 3.5 小时的时间。核心完成两件事：

1. **UI 从 v0.4 → v0.6h 的 8 版迭代**，最终定型为**核仪器黑底风 + 右侧 EC11 旋钮面板**（用户预期的最终形态，旋钮为主触摸为辅）
2. **触摸交互完整修复链**——中间发现 WKS 触摸屏驱动的严重 bug（只发 Pressed 从不发 Released），最终用位置/时间跳变自研 click 判定绕过

---

## SSH 通道恢复（实际时间：07-29 23:00 ~ 23:35）

昨晚归档后系统一直没重启（板子 up 53min 时我 SSH 上去看的），但 SSH 死活连不通——`Permission denied (publickey)`。

**根因诊断**（三层坑串起来才通）：

1. **Windows OpenSSH 10.2 默认不用 ssh-rsa (SHA1) 签名**，但板子 dropbear_2017.75 只支持 ssh-rsa。SSH `-v` 输出 `send_pubkey_test: no mutual signature algorithm`。修复：加 `-o PubkeyAcceptedAlgorithms=+ssh-rsa` 参数。
2. **OpenSSH 需要 .pub 文件才 offer key**：私钥 `D:/simmax/bx71_rsa_key` 存在但 `.pub` 缺失，SSH 日志 `no identity pubkey loaded from D:/simmax/bx71_rsa_key`。修复：`ssh-keygen -y -f bx71_rsa_key > bx71_rsa_key.pub`。
3. **板子上 `/root/.ssh/authorized_keys` 不存在**：从没配到板子上（W2 阶段的 SSH 是怎么通的成谜，或者被清了）。

**破解方法**：用户告诉我 root 密码 = `root`，Python paramiko 密码登录直接写 authorized_keys。

**再踩坑**：paramiko 5.x 内部移除了 ssh-rsa (SHA1) host key，`KeyError: 'ssh-rsa'`。降级到 `paramiko<4` (3.5.1) 才通。

```python
import paramiko
c = paramiko.SSHClient()
c.set_missing_host_key_policy(paramiko.AutoAddPolicy())
c.connect('192.168.6.190', 22, 'root', 'root',
          disabled_algorithms={'pubkeys': ['rsa-sha2-256','rsa-sha2-512']})
c.exec_command("mkdir -p /root/.ssh && echo '<pubkey>' >> /root/.ssh/authorized_keys && chmod 700 /root/.ssh && chmod 600 /root/.ssh/authorized_keys")
```

之后 Windows SSH 免密通了。SSH 全自动化恢复。

---

## UI 迭代 v0.4 → v0.6h（23:35 ~ 02:10）

### v0.4 花哨风被用户拒绝

之前的顶栏 tab + 侧栏 slider + 白底彩按钮 → 用户看图对比说"**你做成了电脑软件，不是仪器**"。参考图是 Rigol DS1104Z。

### v0.5 核仪器黑底风重构

全推翻重写：
- 全黑背景 `#000000` + 深蓝网格 `#003c50` + 蓝绿荧光波形 `#00ffc8`
- 顶栏：`SIMMAX v0.4 · Am-241 · ● RUN · 12:34:56`
- 右侧 100px 软键条（波形/能谱/核素/触发/系统 + 参数）
- 参数子菜单**浮层**（点[参数]从右滑出）
- 底状态栏一行等宽字体参数摘要

部署板子，用户反馈 3 个闪退：
- 拖 slider → 闪退
- 未关子菜单切系统页 → 闪退
- 系统页调参数 → 闪退

### v0.6 干掉浮层，做 EC11 固定面板

用户表态："**先修 bug 再改 UI**"，"**最后的产品我做成旋钮为主触摸为辅**"，要求 8:2 布局右侧放 EC11。

一次到位重构：
- QGridLayout：顶栏 40 + [主 stack 820 + 右 EC11 面板 204] + 底栏 28
- **右 EC11 面板**：参数名 label + 圆形旋钮图 (KnobWidget 自绘, 12/24 刻度 + 黄色指针) + 参数列表 (QStackedWidget 5 页联动) + [◄][●][►] 三按键
- 参数 widgets 全部一次 create，永不 destroy → 无 rebuild 无 crash
- axi table 换 QListWidget + 400ms 定时刷（避免高频 insertRow 崩）

**关键设计**：
- 顶栏 tab 亮暗高亮 + 触摸/旋钮都可切页
- [◄][►] 修改当前选中参数值（-/+）
- [●] 循环切换到下一个参数
- 参数集：波形页(核素/类型/幅度/τ1/τ2/噪声), 能谱页(对数Y/CDF/清除), 触发页(模式/周期/λ/卡方), 核素页(选核素), 系统页(无)

用户看 UI 效果确认"目前 UI 就这样吧"。

### 触摸交互修复 v0.6b ~ v0.6h（8 版）

部署后触摸完全不响应（PC 上鼠标 OK，板子触摸 X）。开始诊断之旅：

#### v0.6b: 师兄补 evdevtouch 插件
师兄从编译机拷贝 `libqevdevtouchplugin.so` 到板子 `/lib/qt5/plugins/generic/`，让我加环境变量 `QT_QPA_GENERIC_PLUGINS=evdevtouch:/dev/input/event1`。

**意外惊喜**：触摸黑闪 bug 消失了！之前定性为便携屏 EMI 硬件问题，现在换 evdev 直读事件（绕开 Qt linuxfb 默认的 mouse-emulate 路径），黑闪消失。**说明 linuxfb 的 mouse-emulate 才是触发 EMI 路径的元凶**，evdev 直接给 Qt 反而干净。

#### v0.6c: 坐标不对齐
触摸能响应但位置不对。用户报告：点右下角 Qt 认为在左上角（中心对称）。加 `screensize=1024x600` 参数 → **Qt 5.9 evdevtouch 不支持这个参数**（是 5.10+ 才加的）。

#### v0.6d: TouchOverlay 视觉反馈
用户提出关键反馈："**你怎么反推我按了哪里？我们通信有延迟，我不可能精细按到某个点**"。这是对我的方法论批评——我不能靠"你按左上→Qt 收到 xxx"来反推坐标映射。

**正确做法**：屏幕上实时画反馈。加 TouchOverlay 类：全屏透明层 + 触摸位置画红十字 + 蓝绿坐标数字，4 秒渐隐。用户眼见为实。

#### v0.6e: overlay 拦截 touch
Overlay 加上后**触摸事件被拦截**——按钮完全不响应。因为 Qt 5.9 的 `WA_TransparentForMouseEvents` **只对 mouse 生效，不对 touch**。

修法：`setEnabled(false)` 让 Qt 完全跳过这个 widget。同时 `QCoreApplication::setAttribute(Qt::AA_SynthesizeMouseForUnhandledTouchEvents, true)` 在 QApplication 之前设，让未处理的 touch 自动合成 mouse。

#### v0.6f: 手动 touch → mouse 合成
上面还是不响应，说明 Qt 内部 touch→mouse 合成有问题。**手动在 event filter 里合成 QMouseEvent 发给 target widget**。

按下按钮**变色**了！但 `clicked()` 信号没触发。用户报告"按钮变色但操作没响应，且后续所有点击无效"。

#### v0.6g: 加详细日志抓包
加 log 每个 touch event 类型 + 每个 touchPoint 的 state。

**关键发现**（用户点了 5 次不同位置）：
```
TE type=194 (TouchBegin)   st=1 (Pressed)  gpt=(1005,523)  ← 第 1 次
TE type=195 (TouchUpdate)  st=2 (Moved)    gpt=(846,543)   ← 第 2 次
TE type=195 (TouchUpdate)  st=2 (Moved)    gpt=(215,12)    ← 第 3 次
TE type=195 (TouchUpdate)  st=2 (Moved)    gpt=(959,241)   ← 第 4 次
TE type=195 (TouchUpdate)  st=2 (Moved)    gpt=(280,17)    ← 第 5 次
```

**触发驱动 bug 定性**：**WKS.cn USB2IIC_CTP_CONTROL 触摸屏只发一次 Pressed，之后全部报告为 Moved，从不发 Released**！

用户手指抬起 5 次，Qt 全程认为"手指一直按着在拖动"。所以：
- 第 1 次收到 MouseButtonPress → button 变色 (但没有 Release → clicked 不触发)
- 第 2-5 次全是 MouseMove → 只更新鼠标位置，button 不 press

#### v0.6h: 位置/时间跳变自研 click 判定
既然驱动不给 Released，我们**不信 state**，用位置+时间跳变检测新点击：

```cpp
qint64 nowMs = QDateTime::currentMSecsSinceEpoch();
int dist = (gpt - sLastGpt).manhattanLength();
qint64 dtMs = nowMs - sLastTs;
bool isNewClick = (sLastTs == 0) || dist > 40 || dtMs > 200;
if (isNewClick) {
    // 立即合成 PRESS + RELEASE (完整 click)
    QMouseEvent mp(QEvent::MouseButtonPress, local, gpt, ...);
    QMouseEvent mr(QEvent::MouseButtonRelease, local, gpt, ...);
    QApplication::sendEvent(target, &mp);
    QApplication::sendEvent(target, &mr);
}
```

**逻辑**：
- 手指真的移动一次不会跳 >40 pixel
- 两次点击之间至少 200ms（人类手速上限）
- 满足任一条件就是新一次 click

**用户反馈**：**"都可以响应了"**。触摸交互全通。

---

## 今日总踩坑 11 个

| # | 坑 | 修复 |
|---|-----|------|
| 1 | Windows OpenSSH 10.2 默认不用 ssh-rsa | `-o PubkeyAcceptedAlgorithms=+ssh-rsa` |
| 2 | OpenSSH 缺 .pub 文件不 offer key | `ssh-keygen -y` 从私钥再生成 |
| 3 | paramiko 5.x 移除 ssh-rsa | 降级到 `paramiko<4` |
| 4 | BusyBox sed `\n` 不换行 | 用 awk 精确插入 |
| 5 | BusyBox dmesg 无 -w | poll 差值 |
| 6 | Qt 5.9 QShortcut 不支持 lambda 构造 | create + connect 分开 |
| 7 | Qt 5.9 evdevtouch 不支持 screensize | 只能用 hw_range_x/y_min/max |
| 8 | Qt `WA_TransparentForMouseEvents` 对 touch 不生效 | `setEnabled(false)` 让 Qt 跳过 |
| 9 | Qt `AA_SynthesizeMouseForUnhandledTouchEvents` 不足以驱动 button | 手动 sendEvent 合成 |
| 10 | **WKS 触摸屏只发 Pressed 不发 Released**（今日最难） | 位置/时间跳变判 click |
| 11 | Qt 3 个 evdev 参数写在 `evdevtouch:/dev/input/event1:invertx:inverty` 中 invert 加了反倒把本来正确的映射搞反了 | 移除 invert，用 TouchOverlay 视觉反馈才发现 |

---

## 关键代码位置

- `D:/Qt_Project/simmax/mainwindow.cpp:` MainWindow::eventFilter（触摸→click 转换核心逻辑）
- `D:/Qt_Project/simmax/mainwindow.cpp:` KnobWidget（自绘 EC11 圆形旋钮）
- `D:/Qt_Project/simmax/mainwindow.cpp:` ParamRow（参数行高亮）
- `D:/Qt_Project/simmax/mainwindow.cpp:` TouchOverlay（触摸反馈层，仍保留用于调试）
- `D:/Qt_Project/simmax/main.cpp:` `AA_SynthesizeMouseForUnhandledTouchEvents` + 字号 12pt
- 板子 `/etc/init.d/simmax`（Qt 环境变量：evdevtouch plugin + logging rules）

## 下一步

- 主项目 git 独立仓库 `xinman-2026-nuclear-pulse-gen` 建仓（继续暂缓）
- 接真 AXI（mmap /dev/mem 或 devmem，师兄 rootfs 待加 devmem 工具）
- 硬件 EC11 旋钮引出到板子 GPIO
- 触摸交互坑抽象进 `fpga-rules.md` 或新建 `qt-embedded-rules.md`

## 交互设计确认（最终形态）

- **EC11 旋钮为主，触摸为辅**（触摸有 EMI 干扰，绕开）
- 三按键 [◄][●][►] 对应旋钮左转/按下/右转
- 每页专属参数集，`[●]` 循环切换
- 顶栏 5 tab 触摸/旋钮都可切
