# 工作日志 - 2026年7月13日

## 初始化工作日志系统

今天搭建了一个工作日志系统，用于记录日常开发历程。

### 完成事项

- **建立工作日志仓库**：在 GitHub 创建了私有仓库 `work-log`，用来存档每天的开发工作日志
- **设置本地同步**：本地工作目录 `00 工作日志` 与 GitHub 仓库关联，方便双向同步
- **配置全局规则**：在 Claude Code 中配置了全局规则，后续涉及开发的工作会自动生成日志记录

### 技术细节

- 日志格式：Markdown，按日期命名（`YYYY-MM-DD.md`）
- 仓库类型：GitHub 私有仓库
- 本地路径：`C:\Users\29035\01 文件夹\学习办公\00 工作日志\`

---

## CoCode v0.2.7 开发

全天密集修改 CoCode（AI 编程助手桌面应用），基于 Tauri v2 + SvelteKit 架构。

### Bug 修复

- **双窗口 + 白窗口 bug**：`tauri.conf.json` 中 `dragDropEnabled: true` 在 Windows 最大化启动时，Tauri v2 的拖放叠加 HWND 的透明属性（WS_EX_LAYERED）失效，变成可见白窗口。改为 `false` 后用 PromptInput 已有的 HTML5 拖放回退自动接管。
- **下拉菜单 UI 重合**：SkillSelector、Mode dropdown 使用 `position:fixed; z-index:50` 导致与其他 UI 重叠。改为 `relative` 容器 + `absolute bottom-full`，不再脱离文档流。
- **"已分叉" 文字竖向排列**：SessionStatusBar 中 forked 按钮缺少 `shrink-0`，在窄窗口下被挤压导致内容换行竖向显示。
- **环境检测 Claude/Codex CLI 找不到**：`check_tool_version` 没有使用 `augmented_path()`，Tauri 进程的 PATH 没有 npm 全局安装目录。改用已有的 `check_agent_cli()` 复用成熟检测逻辑。

### 新增功能

- **默认背景图内置**：将背景图放入 `static/default-bg.jpg`，修改 `+layout.svelte` 的 `applyBackground()` 在无自定义背景时回退到内置默认图，透明度 100%。
- **CLI 导入入口恢复**：侧边栏底部添加上传图标按钮，打开 `CliSessionBrowser` 弹窗，用于从终端 CLI 导入历史对话。
- **版本号显示删除**：侧边栏底部 rail 的 `v0.1` 按钮及其点击打开 License 功能已删除。
- **自动更新检查**：App 启动时调用 Gitea Releases API（`/api/v1/repos/2903537389/CoCode/releases`），比较最新 Tag 与当前版本，有新版本在页面顶部显示蓝色更新横幅。Gitea 实例 v1.16.8，API 公开可访问无需认证。
- **环境检测面板**：设置 → 调试标签页新增环境检测区域（Node.js、Git、Python、Claude Code CLI、Codex CLI），已安装显示版本号，未安装提供官网下载链接。
- **完成提示音**：设置 → 通用标签页新增完成提示音开关 + 自定义音频文件选择。默认用 Web Audio API（880Hz 正弦波 0.3s），支持 mp3/wav/ogg/flac/m4a/aac。

### 版本号

- v0.2.5 → v0.2.7（遵循"每次更新 +0.0.1"的版本规则）

### 技术要点

- **Gitea 仓库**：`http://8.162.7.71:3000/2903537389/CoCode.git`，API v1，无需认证即可读取 releases
- **NSIS 安装包**：覆盖安装不丢失用户数据（localStorage 在 `%LOCALAPPDATA%\com.cocode.desktop\`）
- **环境检测增强**：Windows 上 npm 全局包的 `.cmd` 后缀需要用 `augmented_path()` 来查找
- **Rust 类型一致性**：新增 `UserSettings` 字段需要同时更新 `Default` 实现中的初始化

### 后续计划

- 发布 v0.2.7 Release 到 Gitea（当前尚未创建，所以更新检查还不会触发）
- 测试覆盖安装和自动更新检查流程

---

## FPGA 零基础入门学习（新漫杯·智能仿核信号发生器）

下午开始学习 FPGA，从零起步（无数电/模电/信号处理基础）。赛题是"智能仿核信号发生器"，采用 **STM32F407（主控/UI/能谱算法）+ XC7S6 Spartan-7（CT137X 板，实时波形生成）+ AD9767（双通道 14bit DAC）** 异构架构。STM32 和 FPGA 通过 SPI 通信：STM32 下发波形参数，FPGA 负责 DDS 实时波形合成并输出到 DAC。我的角色是负责 FPGA 端的开发与调试。

### 核心理念建立

**三条工程师直觉**：
1. **不是在写程序，是在描述电路**。`module` = 电路模块（有输入输出引脚的硬件块），不是函数
2. **所有 `always @(posedge clk)` 块同时执行**，硬件天生并行，不是顺序执行
3. **时钟就是心跳**，50MHz = 20ns 周期，所有同步动作都由它驱动

### Verilog 基础语法

| 概念 | 说明 |
|------|------|
| `wire` | 物理连线，不能存值，`assign` 用 |
| `reg` | 寄存器，能存值，`always` 块里用 |
| `=`（阻塞赋值）| 组合逻辑用，立刻生效 |
| `<=`（非阻塞赋值）| 时序逻辑用，clk 边沿同时生效 |
| 分频 | 用计数器数到 N 个周期 = 降速，不是真的改变时钟频率 |

### Vivado 全流程打通

**环境**：Vivado 2018.3 + XC7S6-1ftgb196（Spartan-7）

**解决的工程问题**（独立 debug，4 个真实问题）：
1. **下载器不识别**（"No devices detected"）：JTAG 驱动 + hw_server 僵尸进程锁（PID 21584，`Stop-Process -Force` 杀掉）+ JTAG 排线 Pin1 反插
2. **中文路径报错**：Vivado 2018.3 不支持中文路径 → 源码迁移到 `C:/XiLinxProject/`，Tcl 脚本重建工程
3. **DRC CFGBVS-1 错误**：Spartan-7 必须配置 `CFGBVS VCCO` + `CONFIG_VOLTAGE 3.3`
4. **未用 LED 半亮**：Spartan-7 默认 UNUSEDPIN = PULLDOWN（弱下拉），板子 LED 低电平点亮 → 弱下拉产生微电流半亮 → `BITSTREAM.CONFIG.UNUSEDPIN PULLUP` 根治

**完整工作流掌握**：建工程（选芯片）→ 加 RTL → 加 XDC 约束 → 综合 → 实现 → 生成 bit → 烧录

### Tcl 脚本一键建工程

编写了 `build_led_flow.tcl`：`create_project` → `add_files` → `set_property top` → `launch_runs synth_1` → `launch_runs impl_1 -to_step write_bitstream`，实现命令行一键综合+实现+生成 bitstream。

### LED 流水灯首次上板成功

修改官方例程：LED 从 8 位改为 4 位（`[3:0]`），计数阈值 10M（0.2s 切换），烧录后在板子上观察到 LD1~LD4 依次点亮。

### 工具链配置
- VSCode 编辑 `.v` 和 `.xdc` 文件（语法高亮插件）
- Vivado 2018.3 仅用于综合/实现/烧录
- Tcl Console 替代手动 GUI 操作
- 仿真：XSim（Vivado 内置）
